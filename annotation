eslint faz correções no código js (no caso ts), revisa e corrige erros

ts-node-dev compila as alterações do projeto ao inves de ter que compilar o projeto inteiro toda vez que for roda-lo

prettier é uma ferramenta de suporte de escrita para corrigir despadronização na escrita

yarn --frozen-lockfile fala para instalar exatamente as versões que estão listadas no yarn.lock não instalar versões atualizadas para não dar divergências